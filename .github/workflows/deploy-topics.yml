name: Deploy Topic

on:
  push:
    branches:
      - main
    paths:
      - '.nais/topic-*.yml'
      - '.github/workflows/deploy-topics.yml'

jobs:
  deploy-medlem:
    name: Deploy medlem
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.medlem.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-medlem.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-sokere:
    name: Deploy sokere
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.sokere.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-søkere.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-2:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-2.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-3:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-3.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-4:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-4.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-5:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-4.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-6:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-4.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-12:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-4.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-manuell-11-29:
    name: Deploy manuell
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.manuell-11-4.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-manuell.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-inntekter:
    name: Deploy inntekter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.inntekter.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-inntekter.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true

  deploy-vedtak:
    name: Deploy vedtak
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy aap.vedtak.v1 to dev
        uses: nais/deploy/actions/deploy@v1
        env:
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: dev-gcp
          RESOURCE: .nais/topic-vedtak.yml
          VARS: .nais/dev.yml
          PRINT_PAYLOAD: true
